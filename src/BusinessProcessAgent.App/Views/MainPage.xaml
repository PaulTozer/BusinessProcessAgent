<?xml version="1.0" encoding="UTF-8" ?>
<Page
    x:Class="BusinessProcessAgent.App.Views.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="24" RowSpacing="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Spacing="12">
            <FontIcon Glyph="&#xE7C8;" FontSize="28"/>
            <TextBlock Text="Business Process Agent" FontSize="24" FontWeight="SemiBold"
                       VerticalAlignment="Center"/>
        </StackPanel>

        <!-- Controls -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Spacing="8">
            <ToggleButton x:Name="btnObserve" Content="Start Observing"
                          Click="OnObserveToggle"/>
            <Button x:Name="btnViewProcesses" Content="View Processes"
                    Click="OnViewProcesses" Style="{StaticResource PrimaryAction}"/>
            <Button x:Name="btnSettings" Content="Settings"
                    Click="OnSettings"/>
        </StackPanel>

        <!-- Status -->
        <InfoBar x:Name="statusBar" Grid.Row="2" IsOpen="True" IsClosable="False"
                 Severity="Informational" Title="Ready"
                 Message="Right-click the tray icon to toggle observation. Left-click to open this window."/>

        <!-- Live activity feed -->
        <ListView x:Name="activityFeed" Grid.Row="3"
                  SelectionMode="None">
            <ListView.HeaderTemplate>
                <DataTemplate>
                    <TextBlock Text="Recent Activity" FontWeight="SemiBold"
                               FontSize="16" Margin="0,8,0,8"/>
                </DataTemplate>
            </ListView.HeaderTemplate>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid Padding="8,6" ColumnSpacing="12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <FontIcon Grid.Column="0" Glyph="&#xE7C4;" FontSize="14"
                                  VerticalAlignment="Center"/>
                        <StackPanel Grid.Column="1" Spacing="2">
                            <TextBlock Text="{Binding HighLevelAction}" FontWeight="SemiBold"
                                       TextTrimming="CharacterEllipsis"/>
                            <TextBlock Text="{Binding LowLevelAction}" FontSize="12"
                                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                       TextTrimming="CharacterEllipsis"/>
                        </StackPanel>
                        <TextBlock Grid.Column="2" Text="{Binding ApplicationName}"
                                   FontSize="12" VerticalAlignment="Center"
                                   Foreground="{ThemeResource TextFillColorTertiaryBrush}"/>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
